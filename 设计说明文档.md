# 济事楼导览系统 —— 设计说明文档

**课程作业 Assignment 4：Fluid Navigation**

---

## 一、系统概述

本系统是一个面向同济大学**济事楼 4-5 层**的房间参观导览系统。用户可以：

1. **总览**济事楼 4-5 层全部房间；
2. **快速选择**任意房间查看其用途、人员、开放时间等详细信息；
3. **关键字检索**房间（例如搜索「学工办」「葛蕾」），跨房间号、名称、用途、人员、标签匹配。

技术实现为**静态网页**（HTML + CSS + 原生 JavaScript），无需后端与服务器；同时通过 JavaScript 实现了筛选、检索、详情切换等**动态交互**，属于"动态网页"加分项的范畴。

### 文件结构

| 文件 | 说明 |
|------|------|
| `index.html` | 页面结构 |
| `styles.css` | 视觉样式与响应式布局 |
| `data.js` | 4-5 层共 24 个房间的数据（演示用虚构数据） |
| `app.js` | 楼层切换、分类筛选、关键字检索、详情渲染等交互逻辑 |

> 注：房间用途与人员姓名均为作业演示用的虚构数据，用于展示导览与检索功能。

---

## 二、什么是 Fluid Navigation（流畅导航）

Fluid Navigation 强调用户在界面中的移动应当是**连续、顺滑、低打断**的——用户始终知道自己在哪里、能去哪里，并且在切换内容时不被页面跳转、刷新、上下文丢失所打断。其核心目标是降低导航的**认知负荷**，让"找信息"这件事流动起来，而不是一步步在层级中跳转。

下面结合课件中的 Fluid Navigation 原则，逐条说明本系统的设计理由。

---

## 三、设计原则与对应的设计决策

### 原则 1：保持用户始终知道"自己在哪里"（持续的上下文与方向感）

**设计决策：**
- 顶部标题栏（系统名 + "4-5 层房间导览"）与控制栏（楼层切换、分类筛选）采用 `position: sticky` **始终固定可见**，无论用户向下滚动多少，全局导航与搜索框都不会消失。
- 房间总览按"济事楼 4 楼 / 5 楼"**分组展示**并带楼层标题，用户看到的不是一堆散乱卡片，而是有空间结构的列表。
- 当前选中的房间卡片有高亮边框，详情面板顶部也重复显示"济事楼 X 楼 · XXX 室"。

**理由：** Fluid Navigation 要求用户在移动过程中不迷路。固定的导航锚点和清晰的楼层分组，让用户随时能回答"我在看哪一层、我选中了哪间房"，避免了在长列表中滚动后失去方向。

### 原则 2：减少页面跳转，内容就地更新（最小化打断）

**设计决策：**
- 整个系统是**单页应用（无页面跳转）**。点击房间卡片时，右侧详情面板**就地刷新**，左侧总览列表保持不动。
- 切换楼层、勾选分类、输入关键字时，都是局部 DOM 更新，没有整页刷新或白屏。

**理由：** 传统"列表页 → 点击 → 详情页 → 返回"的模式每一步都是页面跳转，会打断用户的浏览节奏，并丢失滚动位置。Fluid Navigation 主张内容流动而非页面切换——本系统让用户"边看列表边看详情"，浏览多个房间时无需反复前进/后退。

### 原则 3：概览优先，细节按需展开（Progressive Disclosure 渐进式展示）

**设计决策：**
- 默认进入即呈现**全部房间的总览网格**，每张卡片只显示房间号、名称、分类三项最关键信息。
- 详细信息（用途、人员、开放时间、标签）只在用户**主动选择**某房间后，才在详情面板展开。

**理由：** 一次性把所有房间的全部信息平铺会造成信息过载。先给"地图级"的概览满足"快速选择"的需求，再按需提供"街景级"的细节，符合 Fluid Navigation 中"信息分层、逐步深入"的思路，让用户的注意力流畅地从全局聚焦到局部。

### 原则 4：提供多条通往内容的路径（浏览 + 筛选 + 搜索并行）

**设计决策：** 系统同时提供三种到达目标房间的方式，且可叠加组合：
- **浏览**：直接在总览网格中扫视、点选；
- **筛选**：用楼层切换（全部 / 4 楼 / 5 楼）和分类标签（行政办公 / 教学空间 / 会议研讨 / 科研空间 / 服务窗口）缩小范围；
- **搜索**：关键字检索，覆盖房间号、名称、用途、人员姓名、标签——既支持作业要求的「学工办」（按名称），也支持「葛蕾」（按人员）。

**理由：** 不同用户的心智模型不同：有人知道房间名、有人只记得某位老师、有人只想看"所有会议室"。Fluid Navigation 强调不强迫用户走单一路径，多入口让每位用户都能用最顺手的方式流畅抵达目标。

### 原则 5：即时反馈，每一次操作都被"看见"

**设计决策：**
- 搜索结果**随输入实时刷新**（`input` 事件），无需点击"搜索"按钮；命中的关键字在房间号、名称、用途、人员中**黄色高亮（`<mark>`）**。
- 总览标题旁实时显示"共 N 间"的结果计数；无结果时给出友好提示文字。
- 卡片悬停时上浮并加阴影、选中时加高亮边框，详情面板切换时带轻微滑入动画。

**理由：** Fluid Navigation 依赖紧密的"操作—反馈"闭环。实时结果、关键字高亮、计数提示让用户立刻确认"系统听懂了我的意图"，减少了试错与等待，使检索过程连贯顺滑。

### 原则 6：扁平的层级与短路径（降低导航深度）

**设计决策：**
- 信息层级仅为两层：**总览 → 详情**，且二者**同屏并列**（左侧列表、右侧详情），不存在三级以上的菜单。
- 详情面板中的标签（如 `#辅导员`、`#会议室`）可点击，**一键将其作为关键字发起检索**，形成"详情 → 相关房间"的横向跳转。

**理由：** 导航层级越深，用户越容易在中途流失。Fluid Navigation 主张用最少的步骤连接任意两块内容。同屏布局把"点击到看见详情"压缩为一步；可点击标签则提供了内容之间的横向流动路径（例如从「学工办」顺着 `#辅导员` 标签发现其它相关房间）。

### 原则 7：平滑过渡维持空间连续感

**设计决策：**
- 详情面板更新时使用 0.22s 的滑入动画；卡片悬停有 0.16s 的位移过渡。
- 在窄屏（移动端）下选中房间后，详情面板**平滑滚动**到视口内。

**理由：** 突兀的内容替换会让用户瞬间"断片"。适度的过渡动画在新旧状态之间建立视觉连续性，让用户的视线能自然跟随，这正是"流畅"二字的直观体现——但动画时长控制在 0.2s 左右，避免拖慢操作。

### 原则 8：响应式布局，导航体验跨设备一致

**设计决策：**
- 桌面端为"左总览 + 右详情"双栏；屏幕宽度小于 860px 时自动变为单栏上下堆叠，控制栏与卡片网格自适应换行。

**理由：** Fluid Navigation 的"流畅"应不受设备限制。响应式设计保证用户在手机上参观导览时，仍能获得与桌面端一致的浏览—检索—查看详情流程。

---

## 四、界面布局总览

```
┌─────────────────────────────────────────────────────┐
│  顶部栏：系统名 ····················· [ 搜索框 🔍 ]   │ ← sticky 固定
├─────────────────────────────────────────────────────┤
│  控制栏：[全部][4楼][5楼]  [分类筛选标签 ×5]          │ ← sticky 固定
├──────────────────────────────┬──────────────────────┤
│  房间总览（按楼层分组）        │   房间详情             │
│  ┌──────┐┌──────┐┌──────┐    │  ┌────────────────┐  │
│  │ 401  ││ 402  ││ 403  │    │  │ 401 学工办公室  │  │
│  │学工办││团委  ││党委  │ …  │  │ 用途 / 人员 /   │  │
│  └──────┘└──────┘└──────┘    │  │ 时间 / 标签     │  │
│         (可点击)              │  └────────────────┘  │
└──────────────────────────────┴──────────────────────┘
```

布局逻辑：**全局工具（搜索/筛选）置顶常驻 → 内容主体左右并列 → 浏览与详情同屏**，整体形成一条从"找"到"看"的、不被打断的视线动线。

---

## 五、关键交互流程示例

**场景 A：按名称找房间**
输入「学工办」→ 列表实时收敛到 401 室并高亮关键字 → 点击卡片 → 右侧立即显示用途与人员（葛蕾、李明）。全程无页面跳转。

**场景 B：按人员找房间**
输入「葛蕾」→ 系统在房间人员字段中匹配到 401 学工办公室 → 用户由"人"反查到"房间"。

**场景 C：按类别浏览**
点击「4 楼」+ 分类「会议研讨」→ 列表只剩 4 楼会议类房间 → 适合"我想找间会议室"这类模糊需求。

---

## 六、小结

本系统围绕 Fluid Navigation 的核心——**让用户在界面中的移动连续、顺滑、低打断**——进行设计：用常驻导航与楼层分组保持方向感，用单页就地更新消除跳转打断，用概览—详情同屏布局压缩路径，用浏览/筛选/搜索多入口适配不同心智模型，再以实时反馈与平滑过渡维系操作的连贯感。最终让"参观济事楼每一个房间"成为一次流畅、自然的体验。
