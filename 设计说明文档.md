# 济事楼导览系统 —— 设计说明文档

**课程作业 Assignment 4：Fluid Navigation**

---

## 一、系统概述

本系统是面向同济大学**济事楼 4-5 层**的房间参观与导览网站。用户可以：

1. **总览**济事楼 4-5 层全部房间（在真实平面图上一览无余）；
2. **快速选择**任意房间查看其用途、人员、开放时间等详细信息；
3. **关键字检索**房间，覆盖房间号、名称、用途、教师姓名、研究方向、标签等字段（例如「学工办」「葛蕾」「448」「联邦学习」均可命中）。

技术实现为**静态网页**（HTML + CSS + 原生 JavaScript，无任何后端），同时通过 JavaScript 完成**SVG 平面图渲染、悬停提示、搜索高亮、视图切换**等动态交互，覆盖作业要求中"动态网页"的加分项。

### 文件结构

| 文件 | 说明 |
|------|------|
| `index.html` | 页面骨架与全局区块 |
| `styles.css` | 视觉样式与响应式布局 |
| `data.js`    | 房间数据 + 人员数据 + 平面图 SVG 坐标 |
| `app.js`     | 楼层 / 视图切换、分类筛选、检索、平面图渲染、详情面板等交互逻辑 |
| `设计说明文档.md` | 本文档 |

> **数据来源**
> - **人员信息**来自作者整理的 `staff.ts`（济事楼 4-5 层各办公室与教师工作室在岗人员）；
> - **平面图房间分布**严格按用户提供的 4 楼、5 楼平面图截图进行 SVG 重绘，含楼梯、电梯、卫生间与"此路不通"等公共设施标记。

---

## 二、什么是 Fluid Navigation

Fluid Navigation 强调用户在界面中的移动应当是**连续、顺滑、低打断**的——用户始终知道自己在哪里、能去哪里，并且在切换内容时不被页面跳转、刷新、上下文丢失所打断。其核心目标是降低导航的**认知负荷**，让"找信息"这件事流动起来。

下面结合课件中的 Fluid Navigation 原则，逐条说明本系统的设计理由。

---

## 三、核心界面布局

```
┌────────────────────────────────────────────────────────────┐
│  顶部栏：系统名 ································ 搜索框    │ ← sticky 全局
├────────────────────────────────────────────────────────────┤
│  控制栏：[4楼][5楼] · [平面图][列表] · 分类筛选 ×4       │ ← sticky 全局
├──────────────────────────────────────┬─────────────────────┤
│                                      │                     │
│        【SVG 交互式平面图】          │   房间详情面板       │
│   （根据用户提供平面图 1:1 重绘）    │   · 房间号 / 名称    │
│   · 房间可点击 / 悬停                │   · 房间用途         │
│   · 楼梯/电梯/卫生间公共设施         │   · 房间人员卡片     │
│   · 此路不通区域红色标记             │   · 开放时间         │
│   · 搜索时未匹配房间自动变灰         │   · 可点击标签       │
│                                      │                     │
│  图例：[行政办公][教学][科研]…       │                     │
└──────────────────────────────────────┴─────────────────────┘
```

平面图视图与列表视图通过控制栏中的"平面图 / 列表"切换；右侧的详情面板在两种视图下都常驻显示，并随选中的房间联动刷新。

---

## 四、设计原则与对应的设计决策

### 原则 1：保持用户始终知道"自己在哪里"（持续的空间感）

**设计决策：**
- 平面图视图直接呈现**济事楼 4 楼 / 5 楼的真实平面**——房间不是孤立的卡片，而是建筑结构中的具体位置。用户脑中的"济事楼的空间地图"与界面上看到的内容一致。
- 楼层标题（"济事楼四楼平面图"）实时显示在平面图正上方；楼梯、电梯、卫生间等公共设施按平面图实际位置以"楼梯/电梯/卫生间"二字直接标注，作为参照地标进一步强化方向感。
- 选中的房间在平面图上以加粗描边 + 淡色填充 + 阴影**高亮**，用户在浏览详情时仍能一眼看到"这间房在楼层的哪个位置"。
- 顶部栏与控制栏使用 `position: sticky` **始终固定可见**，全局导航锚点不会因滚动消失。

**理由：** Fluid Navigation 要求用户在移动过程中不迷路。**真实平面图 + 持续可见的位置高亮**是最直观的方向感来源，避免了用户在数十个房间号之间记忆位置的认知负担。

### 原则 2：减少页面跳转，内容就地更新（最小化打断）

**设计决策：**
- 整个系统是**单页应用**，全程不发生页面跳转。点击平面图上任一房间，右侧详情面板**就地刷新**；平面图本身保持不动，用户仍能看到当前所选房间在楼层中的位置。
- 楼层切换、视图切换、分类筛选、搜索都是局部 DOM 重绘，无白屏闪烁。

**理由：** 传统"列表 → 详情页 → 返回"的模式每一跳都打断用户节奏。本系统让"看位置"和"看详情"在同一屏内并列发生，浏览多个房间时无需反复前进/后退。

### 原则 3：概览优先，细节按需展开（Progressive Disclosure）

**设计决策：**
- 默认进入即呈现**整层楼的平面图概览**——每间房只显示房间号与分类色块（最少必要信息）。
- 鼠标悬停在房间上时弹出**轻量 tooltip**：房间号、名称、分类、前几位主要人员（中等信息密度）。
- 点击房间才在右侧详情面板展开完整信息：用途、所有人员（含职称、研究方向）、开放时间、标签（完整信息）。

**理由：** 三层信息密度（房间号 → tooltip → 详情面板）让用户的注意力从全局聚焦到局部时是**渐进而平滑**的，避免了一次性的信息过载。

### 原则 4：提供多条通往内容的路径（多入口并行）

**设计决策：** 系统同时提供四条到达目标房间的路径，且可叠加组合：
- **空间浏览**：直接在平面图上扫视、点选房间；
- **列表浏览**：切到"列表"视图，按卡片网格快速浏览；
- **分类筛选**：行政办公 / 教学空间 / 科研空间 / 服务窗口，缩小范围；
- **关键字检索**：覆盖房间号、名称、用途、教师姓名、职称、研究方向、标签——
  - 按名称：「学工办」→ 446
  - 按人员：「葛蕾」→ 446
  - 按编号：「448」→ 副书记办公室
  - 按方向：「联邦学习」→ 507 王洁
  - 按机构：「ACM-ICPC」→ 519

**理由：** 不同用户的心智模型不同：有人记得房间号、有人只记得某位老师、有人只想知道"有哪些科研实验室"。Fluid Navigation 强调不强迫用户走单一路径，多入口让每位用户都能用最顺手的方式抵达目标。

### 原则 5：即时反馈，每一次操作都被"看见"

**设计决策：**
- 搜索结果**随输入实时刷新**——平面图上未命中的房间立即**变灰**、命中的房间保持原色；右上角实时显示"X / 总数 间匹配"。
- 关键字在房间号、名称、用途、人员姓名、研究方向中**黄色高亮（`<mark>`）**。
- 当搜索匹配的房间在另一楼层时，系统**自动切换楼层**并提示，避免"搜了没结果"的错觉。
- 平面图房间悬停时填充色变深、描边变粗；选中时加阴影；视图切换有动画过渡。

**理由：** Fluid Navigation 依赖紧密的"操作—反馈"闭环。**实时灰化非匹配房间**让搜索结果在空间上一目了然，比一行行扫文字列表高效得多。

### 原则 6：扁平层级与短路径（降低导航深度）

**设计决策：**
- 信息层级仅两层：**总览（平面图 / 列表）→ 详情**，且二者**同屏并列**。
- 详情面板中的标签（`#辅导员`、`#联邦学习`、`#院务助理` 等）可点击，**一键以该标签作为关键字反向检索**，形成横向跳转（例如从「学生思政工作中心」顺 `#辅导员` 标签发现其它相关老师所在房间）。
- 键盘快捷键：`/` 聚焦搜索框、`Esc` 取消当前选中。

**理由：** 越深的层级越容易让用户在中途流失。Fluid Navigation 主张用最少的步骤连接任意两块内容；本系统中任意房间—详情—关联房间的跳转都在 1-2 次点击内完成。

### 原则 7：平滑过渡维持空间连续感

**设计决策：**
- 详情面板更新时使用 0.22s 滑入动画；房间悬停 0.18s 平滑过渡。
- 楼层切换、视图切换时局部 DOM 重绘，避免整屏闪烁；选中状态在跨楼层搜索时会自动跟随。
- SVG 平面图使用 `preserveAspectRatio="xMidYMid meet"` 保证在各种屏幕下保持比例、不变形。

**理由：** 突兀的内容替换会让用户瞬间"断片"。适度的过渡动画在新旧状态之间建立视觉连续性——但动画时长控制在 0.2s 左右，避免拖慢操作。

### 原则 8：响应式布局，导航体验跨设备一致

**设计决策：** 当屏幕宽度小于 900px 时，"平面图 + 详情面板"自动从左右双栏变为上下堆叠，分类筛选条自动换行；详情面板在移动端选中房间后平滑滚动到视口内。

**理由：** Fluid Navigation 的"流畅"应不受设备限制——手机上参观导览时仍能获得与桌面端一致的浏览—检索—查看详情流程。

---

## 五、典型交互流程

**场景 A：访客找辅导员葛蕾**
搜索框输入「葛蕾」→ 平面图自动切到 4 楼，446 室保留原色、其它房间变灰 → 点击 446 → 右侧详情显示「学生思政工作中心」+ 主任克热木 + 葛蕾等四位辅导员 + 开放时间。**全程一个搜索、一次点击。**

**场景 B：来访学者找院长办公室**
搜索框输入「院长」→ 450 室高亮 → 点击 → 右侧显示「450R 院长办公室 · 申恒涛 / 多媒体搜索 / 计算机视觉 / 人工智能 / 大数据管理」。

**场景 C：学生找做"联邦学习"的老师**
搜索框输入「联邦学习」→ 平面图上 507 室高亮 → 点击 → 详情面板列出王洁副教授（边缘计算 / 联邦学习 / 网络系统）。

**场景 D：浏览类导览**
切到"列表"视图 → 勾选「科研空间」分类筛选 → 看到 4 楼研究生工作室、计算机系统实验室、服务器机房等，5 楼实验机房、研究生工作室等 → 点击任一进入详情。

**场景 E：通过标签横向发现**
进入 446 学生思政工作中心 → 点击 `#辅导员` 标签 → 即以「辅导员」作为关键字反向检索，平面图高亮所有涉及辅导员的房间。

---

## 六、与课件 Fluid Navigation 原则的对应表

| 课件原则 | 本系统的实现 |
|---|---|
| 保持上下文 / 不迷路 | 真实平面图 + 选中高亮 + sticky 顶部栏 |
| 减少跳转 / 就地更新 | 单页应用、详情面板就地刷新、楼层/视图局部重绘 |
| 渐进式信息展示 | 平面色块 → 悬停 tooltip → 详情面板 三层信息密度 |
| 多入口、多路径 | 平面图点击 / 列表卡片 / 分类筛选 / 关键字搜索 / 标签反向检索 |
| 即时反馈 | 搜索实时灰化非匹配、关键字 mark 高亮、匹配计数实时显示 |
| 扁平层级、短路径 | 仅两级，详情与总览同屏 |
| 平滑过渡 | 滑入动画、悬停过渡、SVG 保持比例 |
| 跨设备一致 | 响应式上下堆叠、移动端自动滚动 |

---

## 七、小结

本系统围绕 Fluid Navigation 的核心——**让用户在界面中的移动连续、顺滑、低打断**——进行设计。最关键的决策是用**真实平面图替代抽象列表**作为主导览：用户的心智模型本就是"济事楼几楼几号房间"，让界面上看到的"地图"和脑中的"地图"严格对齐，浏览—搜索—查看详情的全过程几乎不需要"切换语境"。再配合实时灰化搜索、悬停 tooltip、可点击标签、多入口检索等机制，把一次"参观济事楼"变成一段流畅、自然的体验。
